---
doc_id: ATF-SPRINT-000
doc_type: iteration-plan
version: 1.a
status: draft # [draft / frozen / executing / closed]
linked_design: "./2_算法与架构设计.md"
ai_protocol:
  draft: [brainstorming]
  frozen: [codebase-investigator, writing-plans]
  executing: [using-git-worktrees, dispatching-parallel-agents, subagent-driven-development, test-driven-development]
  closed: [requesting-code-review]
schema:
  task_table:
    columns:
      - {name: "任务编号", type: "text", required: true}
      - {name: "任务描述", type: "text", hint: "若由AI执行，必须包含文件路径和验证命令(如pytest)", required: true}
      - {name: "关联需求", type: "text", required: true}
      - {name: "承担者", type: "text", hint: "AI / 人工", required: true}
      - {name: "预估", type: "estimate", required: true}
      - {name: "状态", type: "text", hint: "[Todo] / [Running] / [Done]", required: true}
---

# 1. 迭代执行计划 (Sprint X)

**更新记录**

| 版本 | 日期 | 作者 | 原因 |
|:---:|:---:|:---:|:---|
| 1.0 | 2026-03-25 | Gemini | ATF-Algo v0.a 初始版本 |
| 1.a | 2026-03-25 | Gemini | 去除花哨术语，回归传统敏捷用词 |

---

## 1. 需求说明 (Requirements)
> **状态锁**：本节完成前，YAML `status` 必须为 `draft`。

### 1.1 计划需求
* **需求编号/名称**：(从《项目计划》中导入，如 FR1)

### 1.2 需求质询记录
| 序号 | 质询点 (矛盾/模糊/边界) | 答复与决策 | 状态 |
|:---:|:---|:---|:---:|
| 1 | | | [Open/Resolved] |

### 1.3 冻结需求 (Frozen Spec)
> 将计划需求拆解为无歧义的执行需求。

| 执行编号 | 需求描述 | 验收标准 (Acceptance Criteria) |
|:---:|:---|:---|
| EX-1 | | |

---

## 2. 调研与方案 (Plan)
> **状态锁**：本节开始前，YAML `status` 必须变更为 `frozen`。

### 2.1 代码调研结论
* **现状痛点**：(发现的代码耦合、依赖问题)

### 2.2 宏观方案设计
> (注：详细的数学推演和性能评估请写在 `./2_算法与架构设计.md` 中)
* **核心思路**：
* **涉及修改的文件**：

---

## 3. 任务与时间表 (Tasking)
> **状态锁**：进入本节前，创建物理沙箱(Worktree)，YAML `status` 变更为 `executing`。

### 3.1 里程碑 (Time Table)
| 里程碑 | 计划日期 | 实际达成 |
|:---|:---:|:---:|
| 任务与设计审核完成 | | |
| 编码与单元测试 (TDD) 完成 | | |

### 3.2 任务分解 (Task Table)
> 任务粒度 <4h (0.5天)。AI 承担的任务必须给出明确的测试命令。

| 任务编号 | 任务描述 | 关联需求 | 承担者 | 预估 | 状态 |
|:---:|:---|:---:|:---:|:---:|:---:|
| T1 | [TDD] 创建 tests/test_demo.py，命令 `pytest tests/test_demo.py` | EX-1 | AI | 1h | [Todo] |
| T2 | [Code] 实现核心逻辑，直至 T1 通过 | EX-1 | AI | 3h | [Todo] |

---
*注：所有任务标记 [Done] 后，将 `status` 变更为 `closed`，进入验收阶段。*
